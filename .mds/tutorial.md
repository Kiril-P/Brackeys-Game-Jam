4. How to Create the Sobel Filter in Godot 4.5Instead of the Unity tutorial's complex HLSL inclusion, Godot shaders are self-contained.Overlay: Create a CanvasLayer with a ColorRect covering the screen.Shader Material: Assign a ShaderMaterial with this logic:Sample the depth at SCREEN_UV.Sample 4 neighboring pixels (offsets).Apply the Sobel kernel: $Edge = \sqrt{Horizontal^2 + Vertical^2}$.Thresholding: Use a uniform float to control thickness so it looks crisp on your RTX 2060.